# Core dependencies
requests>=2.31.0

# Document processing - Docling for advanced PDF processing
# Note: Install Docling dependencies manually to avoid pyobjc compilation issues
# See installation instructions below
PyPDF2>=3.0.0

# Vector store and embeddings
chromadb>=0.4.22
sentence-transformers>=2.3.1

# Additional utilities
numpy>=1.24.0

# Docling dependencies (install manually to avoid pyobjc issues):
# pip install --no-deps docling docling-core docling-parse docling-ibm-models
# pip install pandas beautifulsoup4 lxml python-docx python-pptx openpyxl filetype pypdfium2 rtree marko pylatexenc tabulate
# pip install accelerate pluggy polyfactory jsonlines jsonref latex2mathml torchvision rapidocr